#include <stdio.h>
int main(int argc, char const *argv[])
{
    int multiplicando=2; 
    int multiplicador=2;

    printf("%d x %d es %d\n",multiplicando, multiplicador, multiplicador*multiplicando);
    return 0;
}